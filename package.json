{
  "name": "fool-platform",
  "version": "1.0.0",
  "description": "FOOL Platform — Cognitive Operating System. Phase 1: Core Foundation Layer (contracts, domain, standards, registries, workflow definitions).",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:domain": "vitest run domain/tests",
    "test:architecture": "vitest run testing/architecture"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.17.6"
  }
}
